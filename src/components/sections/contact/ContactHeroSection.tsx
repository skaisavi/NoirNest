import { PageHeader } from "@/components/ui/PageHeader";

export function ContactHeroSection() {
  return (
    <PageHeader
      label="Contact"
      title="Bring the brief, the gap, or the instinct you cannot quite name."
      description="Start with a short note. NoirNest works on focused audits, full launches, and senior design direction for teams that want restraint with momentum."
    />
  );
}
