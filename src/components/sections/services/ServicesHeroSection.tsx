import { PageHeader } from "@/components/ui/PageHeader";

export function ServicesHeroSection() {
  return (
    <PageHeader
      label="Services"
      title="Brand, product, and digital systems for premium launches."
      description="Services are shaped around the exact problem: a focused audit, a full identity system, a digital experience, or the launch layer that connects it all."
    />
  );
}
